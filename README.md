# ФИНСЕЙЛС (бывш. КАРТСЕЙЛС) — витрина карт, вкладов, займов и РКО

Лендинг для клиента **Grigo Vaster** (заказ пришёл через Бота Буратинова, Telegram, 15.09.2026).
Дизайн-референс из ТЗ — **aviasales.ru**: та же логика блоков, перенастроенная с билетов на банковские продукты.

**Боевой домен: http://число.online** (`xn--h1agjk2a.online`, хостинг reg.ru, `bash deploy.sh`).
**Превью для клиента: https://daviddavidso.github.io/kartsales/**

Обновляется пушем в `main` (GitHub Pages отдаёт статику прямо из корня ветки):

```bash
git -c credential.helper='!gh auth git-credential' push origin main
```

## Запуск локально

```bash
python3 -m http.server 4730 --directory /Users/openclaw/kartsales
```

Есть запись в `~/.claude/launch.json` под именем `kartsales` (порт 4730).

## Файлы

| Файл | Что внутри |
|---|---|
| `index.html` | Вся разметка страницы |
| `css/style.css` | Стили и палитра (токены в `:root`) |
| `js/data.js` | **Весь контент офферов, отзывы, FAQ** — правится здесь |
| `js/app.js` | Фильтры, каталог, меню, форма подписки |
| `deploy.sh` | Заливка на FTP клиента |

Доступы к FTP — в `~/.kartsales-ftp` (вне папки проекта). Панель reg.ru создала под домен
две папки (`www/xn--h1agjk2a.online` и `www/число.online`), `deploy.sh` льёт в обе.
Рядом в `www/` лежат другие сайты клиента (naladu.tech, число.space) — не трогать.

## Что из ТЗ сделано

**18.09.2026 — вторая итерация по сообщениям клиента от 17.09:**

- Бренд переименован **КартСейлс → ФинСейлс** (логотип, title, FAQ, подвал).
- Убраны все обещания времени оформления и выдачи («за 15 минут», «решение за 5 минут»,
  «деньги сразу», «круглосуточно») — правило партнёрки для МФО-офферов. Чипс «Деньги сразу»
  стал «Займы онлайн» (id тега `fast` → `online`, старые ссылки `?goal=fast` подхватываются).
- Добавлена категория **РКО для бизнеса** (id `business`): вкладка, пункты меню, плитка
  «С чего начать», бенто-плитка «Бизнесу» (тон `mint`).
- Добавлено 14 офферов, всего **20** (все `trk.ppdu.ru`, ссылки проверены — трекер отдаёт 302
  на лендинг партнёра). Ссылка Ак Барса в скриншоте читалась двояко — верный слаг `u0jgFR1V`.
  У BelkaCredit партнёрка прислала `erid=*` — маркер не выдан, в подписи «erid уточняется».

Офферы по категориям:

| Категория | Офферы |
|---|---|
| Дебетовые | ПСБ «Твой кешбэк», Фора-Банк «Всё включено», ОТП Карта МИР, Ак Барс Карта МИР, «Плати по всему миру» USD |
| Кредитные | КЕБ URBAN CARD, Уралсиб «120 дней», Ренессанс «115 дней», Яндекс Пэй «Супер Сплит» |
| Вклады | Яндекс Пэй «Сейвы» |
| Займы | Max.Credit, Быстроденьги, Простой вопрос, ЛайкЗайм, BelkaCredit, Деньги на дом |
| РКО | Точка, Альфа-Банк (РегБиз + РКО), Россельхозбанк «Свой Бизнес», Банк Инго |

Дизайн снят с живого aviasales.ru: цвета, радиусы и структура блоков взяты
с самой страницы, а не «по мотивам».

| Aviasales | Здесь |
|---|---|
| Плоская синяя шапка `#0c73fe` | Та же шапка, синий `#0b6ae8` (см. ниже про контраст) |
| Крупный заголовок по центру, 44 px | Так же |
| Ряд вкладок «иконка + подпись», активная — белая | Вкладки «Все / Кредитки / Дебетовые / Вклады / Займы» |
| Сегментированная белая строка поиска с оранжевой кнопкой | «Банк / Что важно / Сортировка» + «Подобрать» |
| Светло-серый фон `#f2f4f5`, плитки радиусом 20 без теней | То же |
| Цветные бенто-плитки (персиковая, голубая, тёмная, коралловая) | Четыре плитки под офферы |
| Тёмная плитка с горизонтальной лентой карточек | «Выбор редакции» с лентой из трёх предложений |
| Сетка белых карточек с ценами «от …» | Каталог из шести предложений |
| Строка «Реклама, ООО …, erid: …» под промо | То же под каждым предложением |
| Светлый подвал | То же |

В бенто четыре оффера (по одному на плитку) плюс лента «Выбор редакции»; в каталоге — все 20.

## Как править контент

Всё в `js/data.js`.

- **Добавить оффер** — скопировать блок в `offers`, поменять `id` (латиницей, уникальный), `url`, `erid`.
- **Убрать из «Выбора редакции»** — `pick: false`.
- **Порядок по умолчанию** — поле `popularity` (больше = выше).
- **Быстрые фильтры** — массив `tags` у оффера; список самих фильтров в `D.tags`.

⚠️ `id` менять у уже опубликованного оффера нельзя — по нему строятся id заголовков,
на которые ссылается разметка для скринридеров.

## Что нужно уточнить у клиента

1. **Домен число.online по whois в статусе `clientHold`** — регистратор снял его с делегирования
   (обычно не подтверждены данные владельца). Файлы залиты, но сайт не откроется, пока клиент
   не снимет блокировку в кабинете reg.ru. DNS хостинга уже отдаёт A 31.31.198.61.
2. **erid для BelkaCredit** — партнёрка прислала `erid=*`. Когда выдадут, вписать в `data.js`.
3. **Юрлица рекламодателей.** У каждого оффера есть поле `advertiser` — пустое.
   Заполнить точными названиями из партнёрки, тогда в подписи будет
   «Реклама · ПАО «Банк» · erid: …» вместо «Реклама · erid: …».
4. **Цифры в карточках.** Ставки, лимиты и сроки взяты из публичных описаний продуктов
   и подписаны как справочные. Перед запуском сверить с офферной страницей партнёрки.
5. **Почта в подвале** — сейчас заглушка `info@example.ru`.
6. **Форма подписки** пока без бэкенда: адрес сохраняется в браузере посетителя и
   показывается подтверждение. Если нужны реальные заявки — подключаем `api.php` или бота.

## Доступность

Сайт сделан под WCAG 2.2 AA (в проекте это обязательное требование `CLAUDE.md`).
Автопроверка — 40 сценариев в настоящем Chrome: клавиатура, `Escape` в мобильном меню,
возврат фокуса, объявление количества найденного, сохранение фильтров в адресе,
ошибки формы, размеры кнопок, отсутствие горизонтального скролла на 320 px.

Два цвета оригинала пришлось поправить — у самого Авиасейлса они не проходят AA:

| Пара | У них | У нас |
|---|---|---|
| Белый текст на синей шапке | `#0c73fe` — 4,29:1 ❌ | `#0b6ae8` — 4,95:1 ✅ |
| Текст на оранжевой кнопке | белый на `#fa742d` — 2,76:1 ❌ | чёрный на том же `#fa742d` — 6,75:1 ✅ |

Остальное взято как есть и проходит: чёрный на персиковой `#fdd9c5` — 14,1:1,
на голубой `#bbe6fd` — 14,1:1, на коралловой `#f77482` — 6,9:1, светлый на тёмной
`#331810` — 12,1:1, основной текст на фоне `#f2f4f5` — 16,9:1.

У оранжевой кнопки есть обводка `#7a2e00` (8,6:1 к фону): сама заливка контрастирует
с белым лишь 2,76:1, без обводки кнопку не было бы видно как элемент (SC 1.4.11).

Кольцо фокуса двухцветное (чёрное + белый ореол), поэтому видно и на белом,
и на оранжевом, и на синей шапке.

## Грабли

- `<dialog>` в панели предпросмотра редактора не закрывается по `Escape`: панель шлёт
  событие с пустым `code`, нативный `cancel` не срабатывает. В настоящем Chrome всё работает,
  плюс в `app.js` есть свой обработчик. Проверять меню только в реальном браузере.
- Не добавлять `overflow-x: hidden` на `html`/`body` — сломается `scroll-padding-top`,
  и липкая шапка начнёт закрывать элемент, на который увели фокус.
- reg.ru кеширует `.js` и `.css` надолго, `.htaccess` на это не влияет.
